const footer = document.querySelector('.delivery-footer');
const secondaryNavInFooter = footer.querySelector('.secondary-nav');

if (footer && secondaryNavInFooter) {
	secondaryNavInFooter.classList.add('secondary-nav--in-footer');
}
